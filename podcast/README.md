# Podcast Downloader

Script to download, transcribe, and parse all podcast episodes.

You need these environment variables in `.env`:

- `OPENAI_API_KEY`

Install [uv](https://docs.astral.sh/uv/getting-started/installation/) and [ffmpeg](https://www.ffmpeg.org/), then run:

```bash
uv run main.py
```
